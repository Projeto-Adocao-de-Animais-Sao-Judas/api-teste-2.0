spring:
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
  datasource:
    url: jdbc:mysql://localhost:3306/pessoa_db?useSSL=false
    username: root
    password: root
server:
  port: 8080
  address: localhost
management:
  server:
    port: 9001
    address: localhost
